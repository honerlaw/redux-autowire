{
  "name": "redux-autowire",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "tslint --project tsconfig.json -c tslint.json 'src/**/*.ts'",
    "lint:fix": "npm run lint -- --fix",
    "clean": "rm -rf ./*.js ./*.js.map ./*.d.ts ./async ./decorator",
    "build": "npm run clean && npm run lint && tsc -p ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.20.1",
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "react": "^16.12.0",
    "react-redux": "^7.1.3",
    "redux": "^4.0.5",
    "redux-thunk": "^2.3.0"
  }
}
